<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="app.patient.mapper.PatientMemoMapper">

  <select id="search" resultType="app.patient.entity.PatientMemoEntity">
    SELECT
      memo_id,
      patient_id,
      memo,
      created_by,
      created_at
    FROM patient_memo
    <where>
      <choose>
        <when test="type == 'patientId'">
          TO_CHAR(patient_id) LIKE '%' || #{keyword} || '%'
        </when>
        <when test="type == 'memo'">
          memo LIKE '%' || #{keyword} || '%'
        </when>
        <when test="type == 'createdBy'">
          created_by LIKE '%' || #{keyword} || '%'
        </when>
        <otherwise>
          memo LIKE '%' || #{keyword} || '%'
        </otherwise>
      </choose>
    </where>
    ORDER BY created_at DESC, memo_id DESC
  </select>

</mapper>



